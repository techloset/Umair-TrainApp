import { StyleSheet, color, maskImage } from "react-native"
export const styles = StyleSheet.create({
    container: {

    },
    hover: {
        shadowColor: "#2D9CDB",
        elevation: 20,

        shadowOffset: {
            width: 20,
            height: 40,
        },
        shadowOpacity: 1,
        shadowRadius: 30,

    },

})